import 'package:flutter/material.dart';

/// Global constants class
class Const {
  /// Global navigator key for dialogs and toast which didn't have specific context.
  static late GlobalKey<NavigatorState> navigatorKey;
}
